# Build output (generated by `pnpm build`; regenerated for the file:/link: consumer and on publish)
dist/
# Node
node_modules/
npm-debug.log*
# Editor / OS
.DS_Store
.vscode/
.remember/
# Planning docs (local only)
docs/
# Real mapping overrides + secrets — never committed or published (see mapping.ts)
src/netsapiens.overrides.local.*
.dev.vars
.env
CLAUDE.md
leak-patterns.txt
*.tgz
